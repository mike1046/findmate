// @flow

import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  imageViewerHeader: {
    height: 44,
    width: 44,
    position: 'absolute',
    top: 20,
    left: 0,
  },
});
